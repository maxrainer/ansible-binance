---
# override these variables
# ba_api_url: http://192.168.0.220:5050
# ba_validate_certs: false

# ba_binance_url: "https://api.binance.com/"
api_key: ""
api_secret: ""

min_buy_usd: 10
buy_buffered_usd: 0
sell_multiplicator: 1

# ba_coin: BTC
# ba_interval: 15 Minute
# ba_usd: 10

# no changes below here
ba_coin_action: 
  coin: "['{{ ba_coin }}']"
  interval: "{{ ba_interval }}"

ba_coin_buy:
  coin: "{{ ba_coin }}"
  usd: "{{ ba_usd }}"
  url: "{{ ba_binance_url }}"

# ba_api_key:
#   apikey: "{{ api_key }}"
#   apisecret: "{{ api_secret }}"

ba_endpoint: 
  endpoint: "{{ ba_binance_url }}"

dev_sell: false
dev_buy: false
sell_part: false
sell_all: false
